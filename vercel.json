{
  "buildCommand": "cd travel-app && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "travel-app/build",
  "installCommand": "cd travel-app && npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
